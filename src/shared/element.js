class Element {
  constructor(atomicNumber, name, symbol, color) {
    this.atomicNumber = atomicNumber;
    this.name = name;
    this.symbol = symbol;
    this.color = color;
  }
}

module.exports = Element;
